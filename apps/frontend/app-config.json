{
  "name": "netra-frontend",
  "type": "app",
  "stack": "node20",
  "build_path": ".",
  "build": {
    "command": "npm install --production=false && npm run build"
  },
  "command": "node start.js",
  "env": {
    "NODE_ENV": "production"
  }
}
